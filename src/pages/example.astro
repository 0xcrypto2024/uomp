---
import Layout from '../layouts/Layout.astro';
import { baseUrl } from '../utils/base.ts';
---

<Layout title="股票分析示例 - UOMP" description="从导入到审计的完整 UOMP 操作流程：import、authorize、SDK分析、Gateway远程模式" switchHref={`${baseUrl}en/example/`}>
  <section>
    <div class="container">
      <div class="sec-eyebrow">
        <div class="sec-num">示例</div>
        <span class="sec-tag">从导入到审计的完整流程</span>
      </div>
      <h1>股票分析 Agent</h1>
      <p>
        UOMP 旗舰示例 Agent（v1.0）。升级为<strong>自服务 HTTP API</strong>，可部署到 Digital Ocean，任何人都可以连接并授权分析。分析维度覆盖：盈亏、行业分布、集中度、波动率、Sharpe、VaR、Beta、Alpha、相关矩阵、RSI/MACD 技术指标、再平衡建议、情景分析。
      </p>
      <p>
        完整代码与数据文件在参考实现的 <a href="https://github.com/0xaicrypto/uomp-core/tree/main/examples/stock-analyst" target="_blank" rel="noopener">examples/stock-analyst</a> 目录。
      </p>

      <h2>前置条件</h2>
      <ul>
        <li>Node.js >= 20</li>
        <li>pnpm 9（<code>corepack enable</code> 或 <code>npm install -g pnpm@9</code>）</li>
        <li>已克隆 <a href="https://github.com/0xaicrypto/uomp-core" target="_blank" rel="noopener">uomp-core</a> 仓库</li>
      </ul>

      <h2>1. 构建并初始化</h2>
      <pre><code>pnpm install
pnpm build
pnpm cli init</code></pre>

      <h2>2. 启动 Auth + Guard 服务</h2>
      <p>在<strong>终端 1</strong>启动本地服务：</p>
      <pre><code>pnpm --filter @uomp/server start</code></pre>
      <p>保持该终端运行。</p>

      <h2>3. 导入私有数据</h2>
      <p>在<strong>终端 2</strong>导入风险偏好：</p>
      <pre><code>pnpm cli import ./examples/stock-analyst/sample-risk.json</code></pre>
      <p>导入持仓 CSV（高敏感）：</p>
      <pre><code>pnpm cli import ./examples/stock-analyst/sample-holdings.csv \
  --tag portfolio:holdings \
  --sensitivity high</code></pre>

      <h2>4. 发现并连接 Agent</h2>
      <pre><code>pnpm cli discover ./examples/stock-analyst
pnpm cli connect ./examples/stock-analyst</code></pre>

      <h2>5. 授权 Agent</h2>
      <p>交互式授权（推荐）：</p>
      <pre><code>pnpm cli authorize ./examples/stock-analyst</code></pre>
      <p>按提示选择标签后，CLI 会输出：</p>
      <pre><code>export UOM_TOKEN="&lt;token&gt;"
export UOMP_BASE_URL="http://127.0.0.1:9374"</code></pre>
      <p>脚本化授权：</p>
      <pre><code>pnpm cli authorize ./examples/stock-analyst \
  --scope portfolio:holdings profile:risk \
  --output /tmp/uomp.env</code></pre>

      <h2>6. 运行 Agent</h2>
      <pre><code>source /tmp/uomp.env
node ./examples/stock-analyst/index.js</code></pre>
      <p>Agent 会读取授权数据、获取行情、生成 Markdown 报告到 <code>output/</code>。</p>

      <h2>7. 查看会话与审计</h2>
      <pre><code>pnpm cli sessions -a
pnpm cli audit --limit 20</code></pre>

      <h2>8. 撤销授权</h2>
      <pre><code>pnpm cli revoke &lt;session-id&gt;</code></pre>

      <h2>9. 自服务模式（HTTP API）</h2>
      <p>
        v1.0 新增 HTTP 服务器模式：Agent 作为独立服务运行，提供 Web UI 和 REST API。用户可以直接在浏览器中粘贴 Token 进行分析，无需运行 CLI。
      </p>
      <pre><code>node examples/stock-analyst/server.js
# 访问 http://localhost:3080/ 粘贴 Token 即可
# 或通过 API 调用：
curl -X POST http://localhost:3080/analyze \
  -H 'Content-Type: application/json' \
  -d '&#123;"token":"...","gateway_url":"..."&#125;'</code></pre>
      <p>部署到 Digital Ocean 后即为公共服务，任何人连接即可使用。见 <a href="https://github.com/0xaicrypto/uomp-core/tree/main/examples/stock-analyst/README.md" target="_blank" rel="noopener">完整 README</a>。</p>

      <h2>10. 远程模式：通过 Gateway 访问</h2>
      <p>
        如果 Agent 运行在另一台机器或容器里，可以用 <code>apps/gateway</code> 把本地 Memory Guard 暴露为 HTTPS + mTLS 入口。Agent 会通过 mTLS 连接 Gateway，先做聚合查询，再读取授权数据，最后生成报告、上传 Payload、提交删除证明。
      </p>
      <p>一键端到端测试（含本地模式和远程 Gateway 模式）：</p>
      <pre><code>./scripts/test-stock-analyst.sh</code></pre>
      <p>或手动执行 Gateway 流程：</p>
      <pre><code>./scripts/generate-gateway-certs.sh
node apps/gateway/dist/index.js
# 然后在另一个终端用 --output 授权，设置 UOMP_BASE_URL=https://localhost:9443 运行 Agent</code></pre>
      <p>分析维度：盈亏分析、行业分布（含目标偏差）、集中度 HHI、年化波动率、Beta、止损/止盈信号、回撤告警、组合波动率、中英双语报告。</p>
      <p>完整配置见 <a href="https://github.com/0xaicrypto/uomp-core/tree/main/apps/gateway/README.md" target="_blank" rel="noopener">apps/gateway/README.md</a>。</p>

      <div class="hero-btns" style="margin-top: 2.5rem;">
        <a href="https://github.com/0xaicrypto/uomp-core/tree/main/examples/stock-analyst/README.md" target="_blank" rel="noopener" class="btn-h1">查看完整 README →</a>
        <a href={`${baseUrl}design/`} class="btn-h2">实现设计</a>
      </div>
    </div>
  </section>
</Layout>
